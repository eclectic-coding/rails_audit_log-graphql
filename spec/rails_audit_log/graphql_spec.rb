# frozen_string_literal: true

RSpec.describe RailsAuditLog::Graphql do
  it "has a version number" do
    expect(RailsAuditLog::Graphql::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
